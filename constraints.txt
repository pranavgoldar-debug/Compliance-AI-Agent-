# Render-deploy constraints — exact versions known to resolve & run together
# on Python 3.11 (see render.yaml buildCommand: pip install -c constraints.txt).
#
# Why this exists: an unpinned `pip install -e ".[postgres]"` re-resolves the
# whole dependency tree on every deploy, so a single new release on PyPI (or a
# stale cached build env) can flip the build into ResolutionImpossible. With
# constraints, pip has nothing to solve — it installs this set verbatim.
#
# To refresh: locally run a clean
#   pip install --dry-run -e ".[postgres]"
# and copy the "Would install" versions here.
anthropic==0.109.1
openai==2.41.1
pydantic==2.13.4
pydantic_core==2.46.4
email-validator==2.3.0
pypdf==6.13.2
click==8.4.1
fastapi==0.136.3
starlette==1.2.1
uvicorn==0.49.0
python-dotenv==1.2.2
SQLAlchemy==2.0.50
greenlet==3.5.1
bcrypt==5.0.0
PyJWT==2.13.0
python-multipart==0.0.32
openpyxl==3.1.5
et_xmlfile==2.0.0
httpx==0.28.1
httpcore==1.0.9
h11==0.16.0
slowapi==0.1.9
limits==5.8.0
psycopg2-binary==2.9.12
anyio==4.13.0
sniffio==1.3.1
idna==3.18
certifi==2026.5.20
distro==1.9.0
jiter==0.15.0
tqdm==4.68.2
typing_extensions==4.15.0
typing-inspection==0.4.2
annotated-types==0.7.0
annotated-doc==0.0.4
docstring_parser==0.18.0
Deprecated==1.3.1
wrapt==2.2.1
packaging==26.2
dnspython==2.8.0
PyYAML==6.0.3
httptools==0.8.0
uvloop==0.22.1
watchfiles==1.2.0
websockets==16.0
