# Copy this file to .env and fill in real values.
# .env is git-ignored — never commit it.

# Anthropic API key for live extraction. Required only when running `serve --live`.
ANTHROPIC_API_KEY=sk-ant-...

# Set to 1 to force live mode in the web UI without passing --live on the CLI.
# COMPLIANCE_AGENT_LIVE=1
